#!/usr/bin/env bash

DOCUMENTS=$(xdg-user-dir DOCUMENTS)

tmux new-session -d -s "$1" -n main -c "$DOCUMENTS" \; \
  new-window -t "$1":2 -n doc -c "$DOCUMENTS" \; \
  new-window -t "$1":3 -n file "/usr/local/bin/yazi" \; \
  switch-client -t "$1"
